
import ClientIntakeForm from '../components/forms/ClientIntakeForm';

const formRegistry: Record<string, React.ComponentType<any>> = {
  'client_intake_form': ClientIntakeForm,
  
};

export default formRegistry;
