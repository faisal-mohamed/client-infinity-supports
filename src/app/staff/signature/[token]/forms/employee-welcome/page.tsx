// Re-export the existing onboard form page - it now works with signature tokens
// The onboard API endpoint detects signature tokens and handles them correctly
export { default } from '../../../../onboard/[token]/forms/employee-welcome/page';

